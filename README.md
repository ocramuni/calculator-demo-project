## Setup
pip install -r requirements.txt

## Run tests
pytest

## Run coverage
pytest --cov=calculator

## Lab exercise 
1) Add a parametrized test for the `add` method
2) Check if `ZeroDivisionError` is correctly raised
3) Increase the coverage
